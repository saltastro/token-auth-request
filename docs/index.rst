.. toctree::
   :maxdepth: 2

   installation
   quickstart


token-auth-requests
===================

`token_auth_requests` implements token based authentication for HTTP clients. To find out more, :doc:`install <installation>` the package and read the :doc:`quickstart guide <quickstart>`.
